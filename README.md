# Lock-Free Concurrent Queue — Throughput Benchmark
Lock-free concurrent queue implementation with throughput stress testing.
### ■ Research Highlights
- **Michael–Scott Algorithm**: Implements the non-blocking FIFO queue using compare-and-swap (CAS) atomic ope- **ABA Problem Mitigation**: Addresses the ABA problem using a version counter (tag pointer) appended to ea- **Algorithms Focus**: Benchmarks throughput under varying producer/consumer ratios using Python multiproce**Tech Stack:** Python, `multiprocessing`.
